[project]
name = "pricewatch"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.111.0",
  "supabase>=2.4.0",
  "httpx>=0.27.0",
]

[tool.vercel]
entrypoint = "api/main.py:app"
